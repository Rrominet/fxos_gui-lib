//This is a generated file, don't change it manually, it will be override when rebuild.

case(CLICK) :
    for (const auto& f : _click)
        f(infos);
    break;
case(DOUBLE_CLICK) :
    for (const auto& f : _double_click)
        f(infos);
    break;
case(MOUSE_ENTER) :
    for (const auto& f : _mouse_enter)
        f(infos);
    break;
case(MOUSE_LEAVE) :
    for (const auto& f : _mouse_leave)
        f(infos);
    break;
case(MOUSE_MOVE) :
    for (const auto& f : _mouse_move)
        f(infos);
    break;
case(MOUSE_UP) :
    for (const auto& f : _mouse_up)
        f(infos);
    break;
case(MOUSE_DOWN) :
    for (const auto& f : _mouse_down)
        f(infos);
    break;
case(LEFT_UP) :
    for (const auto& f : _left_up)
        f(infos);
    break;
case(LEFT_DOWN) :
    for (const auto& f : _left_down)
        f(infos);
    break;
case(RIGHT_UP) :
    for (const auto& f : _right_up)
        f(infos);
    break;
case(RIGHT_DOWN) :
    for (const auto& f : _right_down)
        f(infos);
    break;
case(MIDDLE_UP) :
    for (const auto& f : _middle_up)
        f(infos);
    break;
case(MIDDLE_DOWN) :
    for (const auto& f : _middle_down)
        f(infos);
    break;
case(KEY_DOWN) :
    for (const auto& f : _key_down)
        f(infos);
    break;
case(KEY_UP) :
    for (const auto& f : _key_up)
        f(infos);
    break;
case(WHEEL) :
    for (const auto& f : _wheel)
        f(infos);
    break;
case(FOCUS) :
    for (const auto& f : _focus)
        f(infos);
    break;
case(UNFOCUS) :
    for (const auto& f : _unfocus)
        f(infos);
    break;
case(CHANGE) :
    for (const auto& f : _change)
        f(infos);
    break;
case(VALID) :
    for (const auto& f : _valid)
        f(infos);
    break;
case(SHOWN) :
    for (const auto& f : _shown)
        f(infos);
    break;
case(HIDDEN) :
    for (const auto& f : _hidden)
        f(infos);
    break;
case(RESIZE) :
    for (const auto& f : _resize)
        f(infos);
    break;
